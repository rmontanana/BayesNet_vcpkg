name: CI
on: push

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: sudo apt-get install ninja-build cmake
    - run: ninja --version
    - run: cmake --version
    - run: g++ --version
